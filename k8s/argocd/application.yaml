apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vetautet-app
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: vetautet

  source:
    repoURL: https://github.com/TuanPhanDuy/ai-agent-coding-booking.git
    targetRevision: HEAD
    path: k8s
    kustomize: {}

  destination:
    server: https://kubernetes.default.svc
    namespace: vetautet

  syncPolicy:
    automated:
      prune: true        # delete resources removed from Git
      selfHeal: true     # revert manual kubectl changes
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 5
